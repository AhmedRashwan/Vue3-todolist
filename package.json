{
  "scripts": {
    "start": "npm-run-all --parallel serve json-server",
    "serve": "npx serve",
    "json-server": "npx json-server db.json -p 3001"
  },
  "devDependencies": {
    "json-server": "^0.17.3",
    "npm-run-all": "^4.1.5"
  }
}
